apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: auth-dev
data:
  SPRING_PROFILES_ACTIVE: "dev"
  DB_URL: "jdbc:postgresql://postgres-dev:5432/authdb_dev"
  CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:4200"
